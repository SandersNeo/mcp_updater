[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-project-updater"
version = "0.1.0"
description = "MCP Project Updater"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

